import type { Schema } from "../types";

export const schema: Schema = {
  id: "default",
  name: "Schéma cible",
  columns: [
    // TODO: définir les colonnes
    // { key: 'nom', label: 'Nom', required: true, aliases: ['name', 'prénom'] },
  ],
};
